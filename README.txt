to start the game simply:

1. open powershell (standalone or integrated terminal) in this folder
2. type "python -m venv env", there should be a new folder called "env"
3. type ".env/Scripts/activate", there should now be (env) on the left
4. type "pip install tcod", a bunch of text will come up in the terminal
3. type python main.py
4. enjoy